version: '2'

services:
  server:
    image: uoregon-libraries/aspace_publisher:latest
    ports: 
      - '3000:3000'
    environment:
      - SECRET=$SECRET
      - ASPACE_URL=$ASPACE_URL
    command: >
      bash -c "/aspace_publisher/aspace_publisher"
