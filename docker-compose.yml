version: '2'

services:
  server:
    image: golang:1.20.5-bullseye
    ports: 
      - '3000:3000'
    volumes:
      - .:/aspace_publisher
    environment:
      - SECRET=$SECRET
      - ASPACE_URL=$ASPACE_URL
    command: >
      bash -c "/aspace_publisher/aspace_publisher"
